name: Deploy Laravel + Inertia (React) to cPanel via SSH

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.2'
        extensions: pgsql, pdo_pgsql
        tools: composer

    - name: Install Composer dependencies
      run: composer install --no-dev --optimize-autoloader

    - name: Setup Node
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Build frontend assets
      run: |
        npm ci
        npm run build

    - name: Deploy to cPanel via rsync
      uses: burnett01/rsync-deployments@v5
      with:
        switches: -avz --delete
        path: ./
        remote_path: /home/saldomas/reneturos.saldomaster.com/
        remote_host: ${{ secrets.SSH_HOST }}
        remote_user: ${{ secrets.SSH_USER }}
        remote_key:  ${{ secrets.SSH_PRIVATE_KEY }}
        exclude: |
          **/.git*
          **/.github*
          node_modules/**
          tests/**
          .env*
          storage/logs/**

    - name: Run artisan commands on server
      uses: appleboy/ssh-action@v0.1.10
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          cd /home/saldomas/reneturos.saldomaster.com
          php artisan migrate --force
          php artisan config:cache
          php artisan route:cache
          php artisan view:cache
